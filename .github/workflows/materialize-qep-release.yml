name: Materialize QEP Release State

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  materialize:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - run: echo "Workflow temporarily paused for deterministic pipeline surgery."
