name: Rebuild Wave 8 Canonical Corpus CI

on:
  push:
    branches: [rebuild-clean-v1]
    paths:
      - 'rebuild/**'
      - 'data*.js'
      - '.github/workflows/rebuild-wave1-ci.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate-validate-build-smoke:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: rebuild
    steps:
      - name: Checkout full history
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Migrate canonical 249-page corpus
        run: npm run migrate:249
      - name: Validate contracts and provenance
        run: npm run validate
      - name: Build
        run: npm run build
      - name: Smoke runtime
        run: npm run smoke
      - name: Persist validated canonical corpus
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add content/pages.json content/provenance.json
          if git diff --cached --quiet; then
            echo "Canonical 249-page corpus already persisted"
            exit 0
          fi
          git commit -m "wave8: persist validated 249-page canonical corpus [skip ci]"
          git push origin HEAD:rebuild-clean-v1
