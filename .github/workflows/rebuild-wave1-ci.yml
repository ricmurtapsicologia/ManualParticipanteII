name: Rebuild Wave 3 CI

on:
  push:
    branches: [rebuild-clean-v1]
    paths:
      - 'rebuild/**'
      - 'data*.js'
      - '.github/workflows/rebuild-wave1-ci.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migrate-build-smoke:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: rebuild
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
      - name: Install dependencies
        run: npm install --no-audit --no-fund
      - name: Migrate canonical pages 1-25
        run: npm run migrate:25
      - name: Validate contracts
        run: npm run validate
      - name: Build
        run: npm run build
      - name: Smoke runtime
        run: npm run smoke
      - name: Persist validated corpus
        run: |
          if git diff --quiet -- content/pages.json content/provenance.json; then
            echo "Validated corpus already persisted"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add content/pages.json content/provenance.json
          git commit -m "wave3: persist canonical 25-page corpus [skip ci]"
          git push origin HEAD:rebuild-clean-v1
